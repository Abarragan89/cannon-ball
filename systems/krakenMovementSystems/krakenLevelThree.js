const krakenLevelThree = (entities) => {

    // rotates the hinderance
    // if (!entities.cannonBall.isGameOver) {
    //     angleNum += 1
    //     angle = `${angleNum}deg`
    //     entities.longHindOne.rotate = angle
    // }

    return entities;
}

export default krakenLevelThree;