const moveLauncherSystem  = (entities, { touches }) => {

    for (const touch of touches) {
        // console.log(touch)
        // if (touch.event.locationX < 40 ) {
        //     console.log('new')
        // }
    }

    return entities;
}

export default moveLauncherSystem;